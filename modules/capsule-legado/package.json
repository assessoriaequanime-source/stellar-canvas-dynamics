{
  "name": "singulai-capsule-legado",
  "version": "1.0.0",
  "description": "Módulo 1 — Cápsula do Legado Digital: TimeCapsule + LegacyPolicy on-chain",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0",
    "dotenv": "^16.4.0"
  }
}
